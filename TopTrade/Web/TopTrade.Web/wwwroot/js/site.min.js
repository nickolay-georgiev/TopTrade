document.querySelector(".upload-documents-btn").addEventListener("click", n => { n.preventDefault(); const t = document.querySelector(".upload-documents-input"); t.addEventListener("change", n => { n.target.files.length > 0 && document.querySelector(".documents-form").submit() }); t.click() });
